env:
  TERM: xterm-256color

window:
  dimensions:
    columns: 100
    lines: 32

scrolling:
  multiplier: 5

font:
  normal:
    family: Liberation Mono

  size: 10.0

key_bindings:
  - { key: N, mods: Control|Shift, action: SpawnNewInstance }
