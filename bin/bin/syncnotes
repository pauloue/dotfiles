#!/bin/sh

cd ~/Documents/notes || exit

gitstatus=$(git status --porcelain)
if [ "$gitstatus" ]; then
    git add .
    git commit -m "$gitstatus"
    git pull --rebase
    git push
else
    git pull
fi
